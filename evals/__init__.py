"""Offline evaluation harness for the Issue2PR agent.

Suites live under ``evals/suites/<suite>/`` as directories, each containing one
or more task directories with a ``task.json`` ({"issue": ..., "repo": ...}) and
the task's source repository. See :mod:`evals.run`.
"""
